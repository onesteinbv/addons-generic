There's a **Auto Attachment Preview** option in **User Preferences**, where you can
choose between ``yes`` and ``no``. If specified as yes, the preview would be loaded for attachments automatically wherever added else the preview would not be loaded
